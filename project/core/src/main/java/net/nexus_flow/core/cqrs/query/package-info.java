/**
 * Query bus and query-envelope primitives for Nexus Flow.
 *
 * <p>This package contains the immutable query contract, its staged builder, the default envelope
 * implementation, and the bus/handler abstractions used by runtime adapters and application code to
 * dispatch synchronous read-side requests.
 */
@NullMarked
package net.nexus_flow.core.cqrs.query;

import org.jspecify.annotations.NullMarked;
